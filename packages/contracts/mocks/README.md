# Contract Mocks

Canonical fixtures for offline development and UI work.

Prefer importing from `@teto/contracts`:

```ts
import { mocks } from "@teto/contracts";
```

JSON copies are also available in this folder for non-TS consumers.